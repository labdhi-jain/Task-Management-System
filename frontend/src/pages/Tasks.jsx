import React from 'react';

const Tasks = () => {
  return (
    <div className="card" style={{ margin: '2rem auto' }}>
      <h2>My Tasks (Phase 5)</h2>
      <p style={{ color: 'var(--text-secondary)', marginTop: '0.5rem' }}>
        Task CRUD operations, Search, Filtering, Sorting, and Pagination UI will be implemented in Phase 5.
      </p>
    </div>
  );
};

export default Tasks;
